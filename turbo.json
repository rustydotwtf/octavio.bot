{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "../../packages/prompts/prompts/*.md"],
      "outputs": ["dist/**", "prompts/**"]
    },
    "sync": {
      "inputs": ["$TURBO_DEFAULT$", "../../packages/prompts/prompts/*.md"],
      "outputs": ["prompts/**"]
    },
    "check": {
      "dependsOn": ["^check"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^test"],
      "outputs": []
    }
  }
}
