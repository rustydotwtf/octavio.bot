# Assistant secrets
AI_GATEWAY_API_KEY=your_vercel_ai_gateway_api_key
BRAVE_SEARCH_API_KEY=your_brave_search_api_key
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Telegram webhook secret (only required in webhook mode)
# TELEGRAM_WEBHOOK_SECRET=replace-me

# Review CLI / OpenCode secrets (optional for assistant-only local usage)
# GITHUB_TOKEN=
# OPENCODE_API_KEY=

# Non-secret runtime defaults live in ./settings.ts
