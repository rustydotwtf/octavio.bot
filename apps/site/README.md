# octavio.bot site

Static marketing/docs site for shipped Octavio products. The first section covers the Review CLI.

## Local development

From repository root:

```bash
bun install
bun run site:dev
```

From repository root, `bun run dev` starts all app dev scripts (including the site dev loop).

`site:dev` runs a prompt watcher plus the site dev server. Prompt markdown edits regenerate site content automatically; refresh the browser to see changes.

Useful app commands:

- `bun run site:dev`
- `bun run site:sync:watch`
- `bun run --cwd apps/site watch:prompts`
- `bun run --cwd apps/site build`
- `bun run --cwd apps/site preview`
- `bun run --cwd apps/site check`
- `bun run --cwd apps/site smoke`
- `bun run --cwd apps/site sync`

## Content and structure

- `content/` markdown pages (`index.md` maps to `/`)
- `src/ui/` shell/layout components
- `styles/tailwind.css` site theme and typography
- `site.jsonc` navigation and site config
- `assets/` static icons/images

## Prompt sync

- Prompt markdown source of truth is `packages/prompts/prompts/*.md`.
- Site prompt pages in `apps/site/content/prompts-*.md` are generated by `bun run --cwd apps/site sync`.
- Sync preserves existing prompt frontmatter (including policy rules) and adds minimal site metadata (`title`, `group`, `order`) when missing.

## Deploy

This app is initialized for Vercel:

```bash
bun run --cwd apps/site deploy
```

Use `bun run --cwd apps/site build` before deployment if you want to validate generated output locally.
